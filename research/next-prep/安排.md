```
波次0（无依赖，D1 同时起跑）
  ├─ [人] G  手标 gold set（原文→期望 memory semantics→证据→期望命中的查询）
  ├─ [A]  R  仓库骨架 + CI
  ├─ [A]  S  绑定 representation-neutral semantic contract
  └─ [A]  T  turn 存储（单轮持久化，原文保全）

波次1（依赖波次0）
  ├─ [A]  H  评测 harness（喂 gold、判命中）        ← G,R,S
  ├─ [A]  W  wordnet 归一层（term→canonical concept）← S
  └─ [A]  X1 单轮 L1 抽取骨架（turn→memory unit→候选 profile）← S,T

波次2
  ├─ [人] K  representation profile go/no-go（KEOL/AMR/其他）← G,X1,W
  ├─ [A]  X2 单轮抽取接归一 + 对齐 gold 调质量       ← X1,W,H
  └─ [A]  Q0 查询骨架（最傻：按 Concept 全扫返回）    ← X1,H

波次3
  ├─ [A]  M  多轮/跨 session L2 抽象与证据闭包       ← X2
  └─ [A]  Q1 查询加厚：predicate/角色→变量绑定→AND/OR→闭包 ← Q0,W

波次4（实验，你的皇冠）
  ├─ [人] E  embedding 缺口实验 + 解读                ← Q1,X2,H
  └─ [A]  Eh embedding 兜底实现 + 出对比指标          ← Q1,(E的设计)

波次5
  ├─ [A]  F  融合策略落地（大概率"符号主+embedding兜底"）← E 的结论
  └─ [A]  N  snapshot 薄版（raw/revision/profile diff/回看）← X2

波次6
  └─ 端到端 raw turn→memory contract→selected profile→query→trace  ← 全部
```

天	人（判断/实验，走关键判断链）	Agent（建造，走关键构建链） D1 上	起草 gold set 结构，标前 5 条	R 仓库骨架、S 绑 semantic contract 同时开动 D1 下	"标到 12–15 条，附上""期望命中的查询"""	T turn 存储；S 收尾 D2 上	审 Agent 抽取初版，标注哪里表达力不够	H 评测 harness；W wordnet 归一层起步 D2 下	量 wordnet 覆盖率(S2)，喂给 profile 判据	X1 单轮 L1 抽取骨架跑通 gold D3 上	representation profile go/no-go 裁决(K)	X2 单轮接归一、对齐 gold 调质量 D3 下	"定""查询必须支持哪些形态""清单"	Q0 查询骨架（按 Concept 全扫）能返结果 D4 上	审多轮抽取：L2 abstraction 能否回指底层轮次	M 多轮 memory units + 向上抽象 D4 下	一根根点查询加厚轴，逐条验收	Q1 加厚：predicate/role→变量绑定 D5 上	继续验收查询：AND/OR、多跳、接归一	Q1 续：AND/OR/多跳 + 接 W D5 下	设计 embedding 实验：标出表达力缺口、给缺口打标签(细粒度一次性 vs 本体缺失)	Q1 收尾 + 跑纯符号召回基线，出 P/R D6 全	缓冲/补标 gold + 深化缺口标注（吸收前期溢出，1人必留缓冲）	M/Q1 补齐、修基线 bug D7 上	embedding 缺口实验(E)：只在缺口挂 embedding	Eh embedding 兜底实现 D7 下	"解读：embedding 是否只在""细粒度一次性""提升、有没有污染符号精确率"	"Eh 出""符号 vs 符号+embedding""对比表" D8 上	据 E 结论拍板融合策略（fallback 还是加权）	F 融合落地 D8 下	验收 snapshot 是否够用	N snapshot 薄版（快照+diff/回看） D9 全	走查端到端 raw turn→contract→selected profile→query→trace，记问题	端到端串联 + 全 gold 评测 D10 上	写三假设结论 + 下一步投哪	修 bug、补测试 D10 下	定稿规划/结论文档	跑最终评测、留一条可演示链路

当前状态（2026-07-24）：Wave 0 的 10 条 gold-set 候选保存在 `data/gold-candidates/KE-test.json`，仍待人工逐条筛选和裁定。此前自定义 KE v0.1/v0.2 已作废并归档到 `archive/legacy-ke-v0.2/`；本轮按远端可见最新 KEOL 提交 `44631e64fd07c9b85f22e36035bf49c882dba592` 完成 baseline：43 个 user+agent turn、405 个 graph node、371 条 graph edge、164 个 Concept、229 个 Individual、161 个 Operator、309 条 Assertion、774 条 Evidence；共有 323 个 Assertion-turn 证据链接，42/43 个 turn 至少关联一条 Assertion，唯一未关联的是 `TASKMASTER2-CAND-001` 第 6 轮。5 批 normalization 更新 164 Concept/161 Operator，应用 113 个具体 operator schema。KEOL strict validation 为 0 error、0 warning。原生产物、视图和运行记录集中在 `artifacts/keol-baseline/`，适配与编排见 `tools/keol_baseline/`。当前结论只表示 KEOL baseline 的结构和表示能力，不替代人工 gold 标注、operator 粒度审核、嵌套 KE 设计或后续查询评测。当前知识优先抽取与关键词 v3 的正式结果仍保存在 `knowledge-extraction/`，旧关键词批次和 archive 保留供审计，不参与默认展示。

工作区结构化整理已完成并由 `tests/test_project_layout.py` 固化：根目录不再混放候选数据、生成视图、实验脚本或 PDF；知识导出默认读取关键词 v3，重放结果与整理前展示文件 SHA-256 一致；全量测试 241 passed。当前 `.git` 仍为空目录，不具备提交或回滚能力。

AMR 单句表示小实验已完成正式质量/结构生成与双盲评审；它是历史候选表示实验，不再固定后续存储格式。运行 `run-20260724T125522Z` 固定使用平衡型 `gpt-5.6-terra`：C 路线 12 个原子知识、A 路线 12 个直接 AMR、B 路线 12 个知识前置 AMR 均已写入不可变账本；A 为 12/12 首轮 PENMAN 结构有效，B 为 11/12 首轮有效，`AMR-S001` 首轮语法失败已保留并完成唯一一次成功重试。官方 PropBank v3.4.0 frame inventory 下载失败，frame membership 保持 `unavailable`；token、cost 和逐调用 latency 也不可得，因此效率门固定为 `undecidable`。36 个最终候选已由两名隔离 reviewer 在不暴露路线映射的情况下完成双盲评审，程序确认 gold 覆盖完整；当前有 7 个语义结果分歧、0 个 hallucination 集合分歧，状态为 `pending_user`。待裁决清单位于 `artifacts/amr-pilot/reports/adjudication-needed.md`；用户裁决完成前不生成最终分数、gate report 或 AMR 节点通过结论。其他 memory 系统只收集可追溯的作者/官方结果，不在本项目复跑。

本体化记忆优势实验设计已于 2026-07-25 获用户确认，文档位于 `docs/plans/2026-07-25-ontology-oriented-memory-advantage-experiment-design.md`。该实验作为进入原 Wave 4 前的表示/执行能力验证，不把 KE、AMR、WordNet 或三元组预设为唯一底层；按 `L0-L3` 表示层与 `E0-E2` 执行层比较强 dense、建模不足的本体、充分本体和符号优先加 embedding 兜底。首轮固定 60 个英文基础场景，40 个架构导向、20 个中立能力场景，12 个开发、48 个隐藏测试。Mem0、Graphiti、Hindsight、MemPalace 只做版本冻结的官方架构审计和作者结果收集，不在本项目复跑；本项目的消融结果只证明能力类别，不冒充具体系统分数。当前状态为设计完成、尚未实施，后续需另写实施计划和冻结具体 gold。

本体化记忆优势实验已于 2026-07-25 完成 v2 实现与真实模型 hidden 运行，以上一段“尚未实施”状态作废。冻结数据位于 `artifacts/ontology-memory-experiment/gold-v2/`；hidden 运行 `run-20260725T150000Z-real-hidden` 含 48 个场景、1,728 个 arm/track/scale 结果、0 execution error。oracle `O+` 在 exact evidence、答案和约束上均为 1.0，证明充分本体表示与精确执行在受控结构任务上具有能力优势；automatic `O+` 仅为 0.250/0.562/0.500，相对 `B2` 只保留 22.9% oracle 增益，低于 70% 门槛，总 gate 为 `fail`。因此当前决策不是进入 Wave 4 大 benchmark，而是先在 dev 上补 temporal provenance/supersession、conjunction/multihop、alias/sense/absence 和真实 fallback trigger，再以新版本重冻 hidden。`O+E` 本轮触发率为 0，不能据此宣称 embedding 兜底有增益。详细结论见 `artifacts/ontology-memory-experiment/reports/experiment-summary.md`；外部系统仍只保留作者官方结果上下文，不在本项目复跑。

2026-07-25 续：v3 automatic compiler 的 dev-only 修复已完成并记录在 `artifacts/ontology-memory-experiment/reports/v3-dev-compiler-report.md`。修复覆盖 temporal provenance/supersession closure、`linked_to` conjunction/multihop、`sense_of` lexical context 和 explicit absence owner query；`run-20260725T172000Z-v3-diagnostic-dev` 覆盖 dev split 全 arm/track/scale 共 432 rows、0 error、verify valid，automatic `O+`/`O+E` 在 diagnostic dev 上 ESEM/Answer/Constraint 均为 1.0。该结果不是新的 hidden gate；real dense dev 当时未在本地复跑，因为 Windows `D:\Anaconda\python.exe` 缺少 `fastembed`。`O+E` fallback 仍为 0，下一步仍是冻结能真实触发 guarded embedding fallback 的 v3 probe 和新 hidden split。

2026-07-25 再续：v3 fallback probe 已冻结到 `artifacts/ontology-memory-experiment/gold-v3/`，并记录在 `artifacts/ontology-memory-experiment/reports/v3-fallback-probe-report.md`。本版新增 4 个 lexical uncovered-predicate fallback probes（1 dev、3 hidden），总计 64 scenarios、35,200 distractors；`run-20260725T181000Z-v3-diagnostic-all` 覆盖全 split/全 arm/track/scale 共 2,304 rows、0 error、verify valid。automatic `O+E` fallback rate 为 0.0625，structural-family fallback rate 为 0.0；新增 probes 上 `O+` 为 0.0，`O+E` 为 1.0。该结果不是 real dense hidden gate，只说明 guarded fallback 路径已非空并能在 diagnostic encoder 下受约束执行。收尾时修复 `validate-gold` 默认 manifest 行为，未传 `--manifest` 时按 `--source` 所在目录读取 `manifest.json`。下一步仍需恢复 `fastembed` 环境后跑 v3 real dev/hidden。

2026-07-25 续跑：已在 `.venv-dense` 隔离环境中安装 `fastembed==0.8.0` 并完成 v3 real dev/hidden，报告见 `artifacts/ontology-memory-experiment/reports/v3-real-dense-report.md` 和 `v3-real-hidden-gate-report.md`。`run-20260725T190000Z-v3-real-dev` 为 468 rows、0 error、verify valid；`run-20260725T192000Z-v3-real-hidden` 为 1,836 rows、0 error、verify valid。真实 hidden gate 仍为 `fail`，唯一失败门是 `automatic_retained_gain=0.16339869281045752`；automatic `O+E` hidden ESEM/Answer/Constraint 为 0.294/0.627/0.706。真实 fallback 在 3 个 hidden lexical probes 上从 `O+` 的 0.0 提升到 `O+E` 的 1.0，且 structural-family fallback rate 为 0.0；但自动管线整体仍不能进入 LoCoMo、BEAM、LongMemEval 大 benchmark。

2026-07-25 parser 修复续跑：已完成 automatic 表示适配器通用表面式修复，并新增 4 个回归测试。`run-20260725T202000Z-v3-parser2-diagnostic-all` 为 2,304 rows、0 error、verify valid，automatic `O+E` diagnostic ESEM/Answer/Constraint 均为 1.0；`run-20260725T203000Z-v3-parser2-real-dev` 为 468 rows、0 error、verify valid，automatic `O+E` real dev 仍为 1.0；`run-20260725T204000Z-v3-parser2-real-hidden` 为 post-hoc hidden 回归检查，1,836 rows、0 error、verify valid，post-hoc gate 为 `pass`，`automatic_retained_gain=0.8431372549019608`。该 pass 不能替代 clean go/no-go，因为 v3 hidden 已在失败分析中暴露；下一步必须冻结 v4 fresh hidden 后重跑，再决定是否进入 LoCoMo、BEAM、LongMemEval。

2026-07-26 v4 fresh hidden 续跑：已冻结 `artifacts/ontology-memory-experiment/gold-v4/` 并完成正式真实 dense hidden run `run-20260726T021000Z-v4-real-hidden`，共 1,836 rows、0 error、verify valid；模型为 `qdrant/bge-small-en-v1.5-onnx-q@52398278842ec682c6f32300af41344b1c0b0bb2`。gate 报告见 `artifacts/ontology-memory-experiment/reports/v4-real-hidden-gate-report.md`，clean gate 仍为 `fail`，唯一失败门是 `automatic_retained_gain=0.47058823529411764`，低于 0.70。automatic `O+E` hidden ESEM 为 0.5294117647058824，fallback rate 为 0.058823529411764705，structural-family fallback rate 为 0.0；失败集中在 roles/quantity 多取上下文 turn、conjunction/multihop 漏取闭包证据 turn。当前仍不能进入 LoCoMo、BEAM、LongMemEval；下一步是先在 dev/诊断集修复 evidence boundary 和 multihop evidence closure，再新冻 fresh hidden。

2026-07-26 v5 fresh hidden 续跑：已完成 dev/诊断修复、冻结 `artifacts/ontology-memory-experiment/gold-v5/`，并完成真实 dense hidden run `run-20260726T041000Z-v5-real-hidden`，共 1,836 rows、0 error、verify valid。gate 报告见 `artifacts/ontology-memory-experiment/reports/v5-real-hidden-gate-report.md`，clean gate 为 `pass`；automatic `O+E` hidden ESEM/Answer/Constraint 均为 1.0，automatic `O+` 为 0.9411764705882353，`automatic_retained_gain=0.9411764705882353`，fallback rate 为 0.058823529411764705，structural-family fallback rate 为 0.0。该结果允许进入下一阶段预注册自然 benchmark 小切片或扩大受控集；仍不能宣称产品优越性，也不能把本项目消融结果当成 Mem0、Graphiti、Hindsight、MemPalace 的本地分数。

2026-07-26 自然 benchmark 小切片 v1 已冻结：设计见 `docs/designs/2026-07-26-natural-benchmark-slice-design.md`，实施计划见 `docs/plans/2026-07-26-natural-benchmark-slice-plan.md`。原始快照位于 `artifacts/natural-benchmark-slices/raw/`，source manifest 为 `artifacts/natural-benchmark-slices/source-manifest.json`；冻结输出为 `artifacts/natural-benchmark-slices/slice-v1/slice.json` 和 `gold.json`，gold evidence raw-text 映射为 `gold-evidence.json`，候选 evidence corpus 为 `evidence-corpus.json`，外部结果上下文为 `external-results-ledger.json`。slice-v1 共 32 个 item：BEAM 10、LoCoMo 10、LongMemEval 12；LoCoMo category 5 因只有 `adversarial_answer` 而无明确 gold answer，状态为 `manual_required`，不计入 answer correctness。当前验证为 `tests/natural_memory_benchmark` 31 passed，`validate-slice` 与 `validate-ledger` 均 valid。已实现 result/scorer 合同和 `score-results` CLI，可统计 evidence exact、All-Evidence@K、recall、precision、answer exact diagnostic、abstention、critical false positive、fallback、latency/token 指标；真实 FastEmbed `dense_reference` 已运行，结果为 Evidence Set Exact Match 0.375、All-Evidence@10 0.59375、Mean Evidence Recall@10 0.6614583333333334、Mean Evidence Precision@10 0.425、abstention correctness 0.0、critical false positives 2。规则/槽位 `symbolic` v1 已运行，结果为 Evidence Set Exact Match 0.375、All-Evidence@10 0.625、Mean Evidence Recall@10 0.71875、Mean Evidence Precision@10 0.49409722222222224、abstention correctness 0.5、critical false positives 1。guarded `symbolic_fallback` v1 已运行，结果为 Evidence Set Exact Match 0.40625、All-Evidence@10 0.65625、Mean Evidence Recall@10 0.75、Mean Evidence Precision@10 0.5253472222222222、abstention correctness 0.5、critical false positives 1、fallback trigger rate 0.03125；唯一 fallback 项为 `LONGMEMEVAL-6d550036`，找回 4/4 gold evidence。fallback taxonomy v1 已新增，taxonomy 运行指标同 guarded fallback，但结果 metadata 机器化记录 `fallback_decision`、`fallback_reason_candidate`、`fallback_allowed`；本轮 30 项 not_needed、1 项 blocked、1 项 triggered。AMR-style ontology extension 设计备注位于 `docs/designs/2026-07-26-amr-style-ontology-extension-note.md`；当时只把 AMR 风格 predicate-argument/event graph 作为 compiler IR，KEOL 固定落库假设已被后续 representation-agnostic contract 取代。该结果只说明浅层符号和受控 embedding fallback 可部分降低 dense 的过召回/角色绑定错误，并可在空 symbolic 结果上恢复缺失证据；仍未解决 causal answerability、冲突/更新闭包、证据边界和 answer generation/judge。仍不复跑 Mem0、Graphiti、Hindsight、MemPalace、Zep 等外部系统。

2026-07-26 自然 benchmark answerability v2 续跑：已新增 `tools/natural_memory_benchmark/answerability_policy.py` 并接入 `symbolic_fallback_runner.py`，用窄 causal/how gate 区分主题重合与真正可回答的因果关系，同时避免把 “How many projects have I led...” 的 `led` 误判为 `led to` 因果。正式结果为 `artifacts/natural-benchmark-slices/slice-v1/symbolic-fallback-answerability-v2-fastembed-results.json`，评分为 `symbolic-fallback-answerability-v2-fastembed-score.json`，报告为 `symbolic-fallback-answerability-v2-fastembed-report.md`。slice-v1 指标：Evidence Set Exact Match 0.4375、All-Evidence@10 0.6875、Mean Evidence Recall@10 0.78125、Mean Evidence Precision@10 0.5565972222222222、abstention correctness 1.0、critical false positives 0、fallback trigger rate 0.03125；唯一被 answerability gate 拦截的是 `BEAM-100K-C001-abstention-001`，唯一 fallback 仍是 `LONGMEMEVAL-6d550036`。当前 `tests/natural_memory_benchmark` 为 36 passed，`validate-slice` 与 `validate-ledger` 均 valid。该结果支持后续以 AMR-style extended ontology IR 显式建模 causal role、predicate sense 与 evidence completeness，但仍只授权 slice-v1 本项目 arm 分析，不授权完整 benchmark 或产品优势结论。

2026-07-27 事件-角色-证据闭包表示设计已固化：文档为 `docs/designs/2026-07-27-event-role-evidence-closure-representation-design.md`。设计将用户提出的两层建模细化为 `L1 原子事件-角色层 -> linking/normalization/lifecycle/closure 中间层 -> L2 跨轮/跨 session 抽象层`。L1 负责单句/单轮事件、状态、偏好、任务和属性抽取；中间层负责 entity linking、predicate sense、operator normalization、event identity、temporal normalization、lifecycle、closure 和 admission；L2 负责跨轮/跨 session 的 Task、Preference、Project、long-running state 等派生抽象，并必须回指 L1 与 raw evidence。AMR-style graph、KEOL 与其他类型图均为候选 profile，不预选固定持久层。下一步不应直接扩大自然 benchmark，而应先实现最小 IR 合同和诊断集，验证 `led/manage` vs `led-to/cause`、feedback causal answerability、multi-session evidence closure、preference supersession 和 task lifecycle。

2026-07-27 事件-角色-证据闭包最小实现与诊断 runner/report 已完成：实施计划为 `docs/plans/2026-07-27-event-role-evidence-closure-implementation-plan.md`，核心 IR 模块为 `tools/natural_memory_benchmark/semantic_ir.py`，runner/report 为 `tools/natural_memory_benchmark/semantic_ir_runner.py`，测试为 `tests/natural_memory_benchmark/test_semantic_ir.py` 和 `tests/natural_memory_benchmark/test_semantic_ir_runner.py`。当前仅为手写 IR 合同、deterministic executor 和小型诊断报告，不含模型抽取、不改变自然 benchmark slice/gold。正式产物为 `artifacts/natural-benchmark-slices/slice-v1/semantic-ir-diagnostic-results.json` 与 `artifacts/natural-benchmark-slices/slice-v1/semantic-ir-diagnostic-report.md`；运行 `run-20260727Tsemantic-ir-diagnostic` 为 5/5 pass、1 个预期 abstention、0 个 fallback allowed。验证结果：focused semantic IR 13 passed，semantic IR runner 4 passed，`tests/natural_memory_benchmark` 53 passed，`validate-slice` 和 `validate-ledger` 均 valid。下一步应把真实 slice item 映射到 semantic IR，与现有 `symbolic_fallback_answerability_v2` 对齐比较，再决定是否接入模型抽取。

2026-07-27 续：真实 slice semantic IR 映射诊断已完成第一版。计划为 `docs/plans/2026-07-27-real-slice-semantic-ir-mapping-plan.md`，实现为 `tools/natural_memory_benchmark/semantic_ir_slice_runner.py`，测试为 `tests/natural_memory_benchmark/test_semantic_ir_slice_runner.py`。正式产物为 `artifacts/natural-benchmark-slices/slice-v1/semantic-ir-slice-diagnostic-results.json` 与 `semantic-ir-slice-diagnostic-report.md`；运行 `run-20260727Tsemantic-ir-real-slice` 为 5/5 pass。5 个真实冻结 item 覆盖 causal answerability、lexical fallback elimination、conflict evidence closure、update supersession closure 和 temporal chain closure；IR evidence exact 为 5/5，现有 `symbolic_fallback + answerability v2` evidence exact 为 3/5，IR improvement 为 2，现有 fallback triggered 为 1，IR fallback allowed 为 0，IR abstention 为 1。该结果仍是 hand-authored mapping，不含模型抽取、不生成答案、不授权完整 benchmark 或产品优势宣称。验证结果：semantic IR slice runner 4 passed，`tests/natural_memory_benchmark` 57 passed，`validate-slice` 和 `validate-ledger` 均 valid。下一步应扩展真实 item 映射覆盖更多 LoCoMo/BEAM 失败类型，并把相同 L1/L2 bundle 映射到多个候选 profile，比较 round-trip、closure parity 与 query semantics。

2026-07-27 再续：semantic IR 到 KEOL-shaped JSON 的最小 projection adapter 已完成。计划为 `docs/plans/2026-07-27-semantic-ir-keol-projection-plan.md`，实现为 `tools/natural_memory_benchmark/semantic_ir_keol_projection.py`，测试为 `tests/natural_memory_benchmark/test_semantic_ir_keol_projection.py`，产物为 `artifacts/natural-benchmark-slices/slice-v1/semantic-ir-keol-projection.json`。该产物基于 5 个真实 slice IR case，生成 13 条 Evidence、19 条 Assertion，并保留 `semantic_ir_unit_id`、level、predicate sense、source status、lifecycle、role bindings、derived assertion IDs 和 closure metadata。该 adapter 只验证字段映射合同，不写入或修改 `artifacts/keol-baseline/`，不代表 KEOL baseline 已支持完整 IR 自动落库。验证结果：semantic IR KEOL projection 4 passed，`tests/natural_memory_benchmark` 61 passed，`validate-slice` 和 `validate-ledger` 均 valid。该 KEOL projection 后续已完成原生校验与 raw-evidence 回查；它只是一条 optional adapter compatibility track。

2026-07-27 继续：semantic IR KEOL projection 回查链已完成。实现为 `tools/natural_memory_benchmark/semantic_ir_keol_trace.py`，测试为 `tests/natural_memory_benchmark/test_semantic_ir_keol_trace.py`，产物为 `artifacts/natural-benchmark-slices/slice-v1/semantic-ir-keol-trace.json` 与 `semantic-ir-keol-trace-report.md`。报告验证 `Assertion -> Evidence -> source quote` 链路：19 条 Assertion 中 19 条都有 evidence trace，6 条同时有 derived provenance，broken evidence refs 为 0。该结果仍不代表 KEOL runtime ingest、自动抽取或答案生成已经完成。验证结果：semantic IR KEOL trace 4 passed，`tests/natural_memory_benchmark` 65 passed，`validate-slice` 和 `validate-ledger` 均 valid。该历史下一步已被 representation conformance 与 KEOL projection v2 assessment 覆盖，不再作为存储选型主线。

2026-07-27 表示层决策更新：抽取与存储不再强制使用 KE/KEOL。系统固定的是 representation-agnostic memory contract：事件/角色/谓词词义、EvidenceSpan、来源与认识论状态、时间、生命周期、冲突与 supersession、L2 到 L1 派生链、证据闭包、约束执行、版本化 round-trip 和 guarded fallback。KEOL、扩展 AMR、属性图或其他格式都是候选 profile/adapter，必须通过同一 hard gate；标准 AMR 或普通三元组若缺能力只能作为 partial representation。

2026-07-27 conformance 实现：新增 `representation_contract.py`、`representation_conformance_runner.py` 及对应测试。v3 固化 query scopes 与四项新增 hard capability；v4 正式产物为 `representation-conformance-results-v4.json` 与 `representation-conformance-report-v4.md`。Extended-AMR adapter exact round-trip、5/5 query semantic probes、bundle integrity valid，但四项 hard capability 仍 unsupported，因此 `status=fail`、`authoritative_ready=false`。新鲜验证为 `tests/natural_memory_benchmark` 84 passed，`validate-slice` 与 `validate-ledger` 均 valid；较早无 `query_unit_scopes` 的 v1 产物已作废并保留审计。

2026-07-27 KEOL optional adapter 验证：旧 `semantic-ir-keol-projection.json` 通过 KEOL 原生 Pydantic 单项结构验证但有 36 warnings，并有 4 个 closure parity mismatch；它不能视为语义忠实持久化。新 `semantic-ir-keol-projection-v2.json` 在投影前物化 closure，4 complete + 1 incomplete 与 query diagnostics 一致，parity mismatch 为 0，native error 为 0；仍有 30 warnings，且无 reverse round-trip 与 runtime constraint execution，因此分类为 `projection_only`。Extended-AMR v4 已实现但也未通过 authoritative gate；下一步主线是补齐共同 hard capability 或加入另一种完整 profile，而不是直接把任一格式设为数据库。

2026-07-27 authoritative memory contract v3/v5 收尾完成：新增 raw/source/unit revision ledger、`EvidenceSpanV2`、typed L2 structured claims、claim/query closure context、版本化 closure evaluation freshness/result parity 与正式 `run-authoritative-conformance` CLI。不可变产物为 `artifacts/natural-benchmark-slices/slice-v1/representation-conformance-results-v5.json` 和 `representation-conformance-report-v5.md`。正式运行 `run-authoritative-conformance-v5` 的 source validation 有效并重放 13 个 source record，closure fresh/result parity 为 6/6，frozen correctness 为 5/5；五项独立 oracle SHA-256 为 `cfebd096c369c4a982e9db0b09cb4b32a98cb165853f005cf52fe049a45d8659`，覆盖 matched claim，并由 stale/incomplete active-L2 负向探针、逐 carrier correctness 和 measured capability gates 加固。Native v3 与 Extended-AMR v2 均 exact round-trip、5/5 query parity，但 LongMemEval count case 因 project identity 未解决而正确以 `structured_l2_identity_unresolved` abstain，所以两种 carrier 都保持 `authoritative_ready=false`。最终存储表示仍未选择；本轮只完成统一测量仪器和基础语义契约，不证明 AMR、KEOL、本体记忆或本项目优于主流 memory 系统，embedding fallback 仍不得作为事实权威。正式结果/报告哈希分别为 `3ec6656c037200f8591fac44cd7e1e4bf2caa3aa9447f3fa5ba9854324d4cc33`、`9ef73e7a4a16409ef9f5edbb74e38b2c1b1b628821a4b13ce2bc0698f03a011a`，隔离回放字节一致，完整 `tests/natural_memory_benchmark` 为 117 passed。

2026-07-27 identity TODO 波次完成：Extended-AMR 已作为主候选 carrier，权威身份和聚合仍由表示无关 v4 合同约束。已冻结 schema.org 30.0 官方参考、4 dev + 4 hidden 手写 identity 场景和预注册 gate；实现 `identity_resolution.py`、`extended_amr_v3_adapter.py`、`identity_conformance_runner.py` 与 `run-identity-conformance` CLI。正式运行 `run-20260727T080000Z-identity-v1` 为 `pass`：0 critical false merge，answerable `count_distinct`、evidence set、abstention、revision、closure freshness 均 1.0，structural fallback 0，Native v4 与 Extended-AMR v3 round-trip/query parity 均 1.0。结果/报告哈希为 `e7cc632ae20f6436156fcd9c777352eac0f8286e63cb24f42ddee05875ae633c`、`bc032cf6a5590a8d1f12adf5d523af9da65d599b66ceddac52667d213b920ae8`，隔离重放字节一致；完整自然 memory 测试 133 passed。当前 TODO 转为：预注册更大的自然 identity/membership slice；评测模型提出 identity candidate 的 false-merge/abstention 质量；只有程序化 closure/policy gate 可接受 merge。冻结 v5 LongMemEval 身份仍未解决，不得用诊断样例强行改成 count=2。

2026-07-27 natural identity/membership proposal v1 完成：冻结 12 个自然文本 case（6 dev + 6 hidden），并实现严格 public/authority/gold 分层、来源 quote/LoCoMo actor 重放、public-only deterministic reference proposer、authority gate、raw/gated scorer、报告和四个 CLI。正式运行 `run-20260727T090000Z-natural-identity-reference-v1` 的 `gate_safety_ready=true`、`proposal_quality_ready=false`；raw accuracy 0.8333333333333334、raw critical false merge 1、gate intervention 2，gated accuracy 1.0、gated critical false merge/membership 均 0、evidence exact 1.0、structural fallback 0。两个 intervention 都只降为 abstain，不做无证据的反向纠正。正式 score/report 哈希为 `d346eb0f0d277be39fad363da8f4f89f98b0fc4a49be2a95c0970f62abf562b2`、`45d0dd7452873e0df8545b9930b19aad6938b2ea764cc2177a16de3e05ba380a`，7 个文件隔离重放字节一致；focused 10 passed、完整自然 memory 143 passed、slice/ledger valid，冻结 identity v1 与 v5 哈希不变。`核心影响：无`。其后真实 proposal-only 模型运行已完成，但因 public 标识符含结果暗示而不能作为 clean model-quality evidence；仍不得授权自动 merge/membership 写入，真实 LongMemEval project identity 保持 unresolved。

2026-07-27 H100 真实 proposal-only 模型运行完成：隔离 proposer `codex-gpt-5.6-sol@2026-07-27` 仅读取冻结 `public.json`，生成 `run-20260727T104020Z-codex-gpt-5-6-sol-proposal-only-v1` 的 12 条候选。scorer 机械双门均通过：raw/gated accuracy、raw abstention F1、proposal/required evidence exact 均为 1.0；raw/gated critical false merge/membership、gate intervention、structural fallback 与失败 case 均为 0。public/authority/gold/manifest SHA-256 为 `2020abb0f13cb91648e0bdf64eb53cc27b3ca8b12783e3aedba0280280270cf3`、`c5a877ea2420ec65aa41020d77febaba6314528ef7586abfe1c42dbeaebf8985`、`347f185e7364920a1a041074dfb9a74de7585111cb49d726336e3efb97df839b`、`023c6a06ddab7d71d21c8483d48da7b655d245ebf41265cbdd021f4eafd75d0d`；proposal 文件/canonical hash 为 `69fb067e6352f23c7b2abc873653545a82ed0f9f65f3f9ef668f8631b141a376`/`d42622f050d39d398a1d424efa91227395b3e340c7a65d88ac8f5d106feaa4b8`，score/report 文件哈希为 `94d804b3ba2b25acd77c1173e3fb4835778e36e99ac164088c52c3edb87eef9f`、`194037cda17a4466ba2d9ee23d7235884b958c3f347fed75867d4df493f6c1b7`，原位 scorer 重放一致且三份产物只读。只读复审发现 `public.json` 的 `case_id`/`mention_id` 含 `distinct`、`mismatch`、`member`、`unresolved` 等 outcome-bearing token，可能向 proposer 暗示预期动作。因此 `gate_safety_ready=true` 仍可作为安全诊断，`proposal_quality_ready=true` 只能解释为 scorer 机械值；clean model-quality evidence 尚未取得，v1 public 与 run 保持不可变审计状态。`.venv-h100` 已创建，focused 10 passed、完整 natural memory 143 passed，natural identity/slice/ledger valid，受保护 identity-v1、authoritative-v5 与 reference 哈希不变。H100 迁移仅修正两处测试路径 fixture；`核心影响：无`。候选仍非权威，不授权自动 merge/membership/L2 写入，`LONGMEMEVAL-6d550036` 保持 `structured_l2_identity_unresolved`。下一 TODO：预注册并冻结 opaque-ID v2 slice，再由新的隔离 proposer 重跑；不得从本次机械 gate pass 进入自动接受路径或外推为存储、产品、UX、外部系统优势。

2026-07-27 opaque-ID v2 clean run 完成：新增 deterministic opaque remapping 与 public-only model-run freeze/provenance 工具，冻结 `natural-v2` 12-case slice（6 dev + 6 hidden），public ID 无 outcome-bearing token且与 v1 控制语义等价。隔离 proposer `codex-gpt-5.6-sol@2026-07-27` 仅读取冻结 prompt/public，proposal/provenance 在 scoring 前冻结；proposal SHA-256 为 `55034bc117c589baf434c6181439eaa57e3158b9a404f9ffa102769a1c5d7f8d`。独立 scorer 结果为 `gate_safety_ready=true`、`proposal_quality_ready=false`：raw accuracy `0.8333333333333334`、critical false merge `1`、critical false membership `0`、abstention F1 `0.0`、evidence exact `1.0`；dev repository/application identity 被错误 merge，hidden LongMemEval project identity 被错误 keep-distinct，两个 gold 都是 abstain。gate 两次 intervention 均降为 abstain，gated accuracy `1.0`、gated critical error `0`，但不能掩盖 raw 失败。score/report SHA-256 为 `3124ade877c4aaf8d81518f75587ac219acc1ff8060b80acfadff06650d8875e`、`76ae0df98040eb269467ca4a4454955434e7bc8f899455d0abd7d76466560037`，重放字节一致。最终审查后 validator 已补固定 namespace/派生 ID 重算、v1 -> v2 derived artifact canonical equivalence、strict preregistration boundary 与只读权限校验；focused 27 passed、完整 natural memory 160 passed、opaque/natural identity/slice/ledger valid、13 项受保护哈希不变，正式 v2 文件全部 `0444`。`核心影响：无`。下一步只用 dev/诊断数据修复 false merge/abstention，hidden 不得用于调参；之后冻结新版本和 fresh hidden。候选仍非权威，`LONGMEMEVAL-6d550036` 保持 `structured_l2_identity_unresolved`。

2026-07-27 dev repair 与 fresh-v3 完成：policy-v2 dev raw quality 失败后，只用 dev false merge 收紧 coreference 规则并冻结 policy-v3；passing dev run 的 raw accuracy、abstention F1、evidence exact 均为 1.0，critical false merge/membership 为 0/0。policy/final prompt 先冻结，之后新建 `natural-v3-fresh` 6 hidden；formal 12-case slice 的六类 hidden action 各 1、3 个 cross-session，v2 ID/source-ID/evidence overlap 均为 0。final proposer `run-20260727T160000Z-claude-sonnet-4-6-fresh-v3` 先冻结 proposals/provenance 再评分：raw `proposal_quality_ready=true`，accuracy `0.9166666666666666`、critical false merge/membership `0/0`、abstention F1 `0.8571428571428571`、evidence exact `1.0`；完整 `gate_safety_ready=false`，唯一错误是 hidden membership actor mismatch 被错误 abstain，gate 不反向补写 exclude，gated accuracy 同为 `0.9166666666666666`。最终审查补强了两项污染控制：dev source 必须只读并与冻结 opaque-v2 source 字节一致；正式 chronology 必须满足 policy freeze < 完整 passing dev run < hidden source，且由只读 `chronology-receipt-v3.json` 的固定哈希绑定文件哈希与 mtime；正式 fresh root 强制使用正式 v2/policy/hidden 路径，不能以字节相同的临时副本绕过收据。该收据是 posthoc filesystem audit，不是外部可信时间戳，declarative isolation 的审计限制不变。focused 42 passed、完整 natural memory 175 passed，fresh/opaque/base/slice/ledger validators valid，25 项受保护哈希不变，26 个正式与审计文件均不可写。`核心影响：无`。candidate generation 只保留 immutable proposal queue -> unchanged gate 的候选设计，暂不接入；fresh hidden 不得用于修改 policy-v3，不授权自动 merge/membership/L2 写入，LongMemEval identity 继续 unresolved。

## H100 迁移后的 TODO（2026-07-27）

- [x] 完成本地工作区瘦身；两次清理报告均为 `cleaned` 且无剩余目标。
- [x] 将冻结资料迁移到 H100 `/public/home/wwb/KE-mem/KE-memory-next-prep-20260727`，核对 6,586 个文件、7,426 个条目、1,412,143,854 bytes 和六项受保护哈希。
- [x] 冻结本地目录为 audit-only；此后不在本地运行新实验或继续实现。
- [x] 在 H100 创建 Linux 专用 `.venv-h100`；未复用或修改迁移中的 Windows `.venv-dense`。
- [x] 启动全新隔离 proposer，只允许其读取 `artifacts/identity-memory-experiment/natural-v1/public.json`，生成完整 12-case `natural-identity-proposals-v1`。
- [x] proposer 输出冻结后，再由 scoring 阶段读取 authority/gold 并运行现有 `score-identity-proposals`；未提前向 proposer 暴露 gold、authority、reference proposals/report/score 或历史 scorer 输出。
- [x] 记录 scorer 机械结果：五项 proposer-quality gate 全部通过，且 `gate_safety_ready=true`；随后确认 v1 public ID 含结果暗示，因此这不是 clean model-quality evidence。
- [x] 候选保持非权威；未增加自动 merge/membership 写入路径，`structured_l2_identity_unresolved` 继续保留。
- [x] 预注册并冻结 opaque-ID v2 natural identity/membership slice；所有 proposer 可见的非语义标识符不含动作、标签或答案暗示。
- [x] 使用新的隔离 proposer 仅读取 v2 public 文件重跑，proposal 冻结后再由 scorer 读取 v2 authority/gold；raw quality fail 与 gated safety pass 已分开记录。
- [x] 仅用 dev/诊断数据修复 false merge 与 abstention；policy-v3 dev gate 已通过，并在 policy/final prompt 冻结后完成 fresh-v3 评测。
- [x] 在独立 dev/诊断数据上验证 actor-binding mismatch 覆盖；policy-v4 首轮 false abstention 只在 dev 修复，policy-v4.1 dev 双门通过后才冻结并评测 fresh-v4。
- [x] 完成 6-case hidden-only fresh-v4：六类 action 各 1、3 个 cross-session、prior evidence/ID overlap 为 0；最终隔离 proposer 的 raw quality 与 deterministic gate safety 均为 1.0，critical false merge/membership 为 0/0。
- [x] 只读评估 candidate generation 接入现有 identity pipeline：已实现 frozen `public/proposals/score -> immutable review queue -> read-only compatibility assessment`，保持 deterministic gate 不变，未授权自动 merge/membership/L2 写入。
- [x] 完成现有真实模型抽取到 authoritative L1/L2 contract 的只读 bridge assessment；未重跑模型，未授权自动写入。
- [x] 只在 dev/诊断数据上完成 typed extractor v2 L1 qualification；raw proposer quality 与 deterministic gate safety 均已通过，未创建 fresh hidden。
- [x] 完成 typed extractor v2 L2 dev qualification；最终 v9/v12 的 raw proposer quality 与 deterministic gate safety 均通过，尚未创建 fresh hidden evaluation。
- [ ] typed extraction 稳定后继续问题编译、证据闭包、跨 session 聚合和真实 benchmark 扩展。
- [ ] 最后比较 Extended-AMR、KEOL projection 或其他 profile 的存储效率、执行能力与运维成本，不预先指定最终格式；核心骨架非必要不得动。

2026-07-28 candidate-generation integration assessment v3 完成：新增独立 assessment 模块与 CLI，只消费 frozen fresh-v4 public/proposals/score，不读取 authority/gold、不调用 proposer、不生成权威 `IdentityDecision`。正式队列为 4 个 `eligible_for_manual_review` 与 2 个 `gate_abstained`；raw proposer quality 与 deterministic gate safety 均通过并继续分开报告。case/mention mapping rate 为 `1.0/1.0`，existing entity、L1 evidence、source revision 和 evidence-chain binding rate 均为 `0.0`，ambiguous L1/source binding 均为 `0`，authoritative materialization ready count 为 `0`，blocked authoritative writes 为 `4`，automatic authoritative writes 为 `0`。复审补充 duplicate public case/mention 拒绝、严格 score/metric/claim/hash/readiness 校验、按 subject reference 计数、唯一 L1-to-source evidence-chain 绑定，以及所有受保护写入面的显式 false claim；输出在成功和部分失败路径上均立即冻结。guard bundle fingerprint 前后均为 `0f00f17b9ed5bd95921ecc2734d29692872e2c4be85c351eee3d2214f8133242`，counts 不变；`candidate_generation_integration_ready=false`。正式只读产物位于 `artifacts/identity-memory-experiment/candidate-generation-assessment-v3/`，queue/assessment/report SHA-256 为 `518ead9de8627a9a4384df8cdd9b8728a21fcf797f6b0f3d208dfcb28ac41c0f`、`49ef708c77561680703e578de140a9d664a0a774dd48dfda0af038bbdc09c7cf`、`a4ab8ac584bfde78469244c42e8adc63cd79eecbe14a6eb27f636b9fd74f1b3b`，均为 `0444` 且原位重放字节一致；v1/v2 保留为不可变复审审计产物。最终新鲜验证为 focused assessment `16 passed`、全部 identity `82 passed`、完整 natural benchmark `219 passed`、`compileall` 成功；natural-v1、opaque-v2、fresh-v3、dev-v4、fresh-v4、slice-v1 和 ledger validators 均为 `valid`，fresh-v4 受保护 proposal/score/report 哈希不变。下一阶段按既定顺序进入自动 L1/L2 抽取、问题编译、证据闭包、跨 session 聚合与真实 benchmark 扩展；不得把 review eligibility 解释为自动写入许可。

2026-07-28 automatic L1/L2 extraction bridge assessment v3 完成：新增严格只读的 replay、compatibility envelope、assessment/report 与 `assess-automatic-l1-l2-extraction` CLI，直接复用冻结的 43 个 turn-pass、10 个 dialogue reconciliation 和 416 条 projected knowledge，没有重跑模型。Raw extraction structure 为 416 projected、390 active、402 single-turn、14 cross-turn、436 evidence references，exact evidence binding/source-status admissibility/required surface completeness 均为 `1.0`，object present 为 `397/416`，condition/scope/non-explicit derivation records 为 `51/79/68`；这些是结构与来源绑定指标，不是 gold semantic accuracy。Deterministic bridge safety 为 L1/L2 candidates `402/14`、authoritative-ready `0/0`、materialization blocked `416`、所有 automatic writes `0`。memory kind、predicate sense、canonical operator、typed roles、local entity IDs 各缺 `416`，typed time `64`，unsupported modality `52`，typed condition/scope/derivation gaps 为 `51/79/68`；14 个 L2 全部缺 supporting L1 IDs、structured claim、abstraction method、closure 和 source turn/session coverage。guard fingerprint 前后均为 `e184b6caf2998acf1c8700bc84d24bbafd49ab9c8f15738d1af8cc484ee5ebcc`，`automatic_extraction_integration_ready=false`。

bridge-v1/v2 保留为不可变审计结果；v1 taxonomy 不完整，v2 仍漏验 `confirmed_by`/`added_by` operation ownership。v3 从冻结 dialogue validated files 重建 operation ID -> candidate 映射，拒绝 unknown/cross-candidate confirm/add 引用并逐 envelope 保留 operation provenance，同时补齐 condition/scope/derivation/speaker、精确 coverage、optional object 和 evidence mismatch 故障路径。正式 `artifacts/automatic-extraction-assessment/bridge-v3/` ledger/assessment/report SHA-256 为 `2ed9e6fdeaca81964fff542287adfc2980145b978b7ef9ed6dfc5f914ebca7c0`、`d5d00b2edebdaf7b2409f09a647c0bce41d1ce29e5c2e1d3c67e646fb5f5e92e`、`b7e29b605ac46b0ff85c1be963f9d3fca7693f3c445a64188f67b8eecc3d9bf9`，均为 `0444` 且隔离重放字节一致，bridge-v1/v2 哈希未漂移。最终验证为 focused bridge `51 passed`、knowledge pipeline `197 passed`、natural benchmark `270 passed`、`compileall` 成功。下一项按冻结详细计划只在 dev/诊断数据上执行 typed extractor v2 的 L1 qualification；L1/L2 dev gate 通过前不得创建 fresh hidden evaluation，也不得自动写入 L1/L2/revision/closure/identity/membership。

2026-07-28 typed extractor v2 L1 dev qualification 完成。第一版 prompt 未给出精确 proposal schema，staged output 在 freeze 前被拒绝；其后 dev-v1 与 dev-v2 的 raw failure/gate safety pass 分别按 taxonomy 保留，没有用 gated 安全覆盖 raw 错误。最终 formal root 为 `artifacts/automatic-extraction-assessment/typed-extractor-v2-dev-v3/`，passing run `run-20260728T060140Z-deepseek-v4-pro-typed-l1-dev-v6` 实际使用 `deepseek-v4-pro` 的全新无历史 OpenAI-compatible request，只读取 frozen prompt/public，proposals/provenance 在 authority/gold scoring 前冻结。raw proposer quality 与 deterministic gate safety 均通过：decision accuracy、abstention F1、evidence、kind、predicate/operator、modality/polarity、time、lifecycle、derivation/speaker 和 operation provenance 均为 `1.0`；role/local-entity 与 condition/scope 为 `0.8888888888888888`，唯一残留是一个 condition participant/local-entity error；critical false emission、gate intervention、deterministic critical false materialization 均为 `0`。guard fingerprint 前后均为 `e184b6caf2998acf1c8700bc84d24bbafd49ab9c8f15738d1af8cc484ee5ebcc`，所有 automatic L1/L2/revision/closure/identity/membership writes 为 `0`，`LONGMEMEVAL-6d550036` 保持 `structured_l2_identity_unresolved`。final proposals/score/report SHA-256 为 `7c765a4185c4b28ecdbb0bf23702e0bbf1931c7c11a4d9b5c420bf80b3952899`、`db0fcc05e9ab3a4c64eac45062eb1a35447e7740b2566baa5fbd8575a3bcbefe`、`c1f088f8e13b962cd4a1ea2719ac4aee8f38a3ef4ef2c1d7fb7c6d3bed6e1ace`，均为 `0444` 且 score/report/error-analysis 重放一致。最终验证为 focused `22 passed`、knowledge `197 passed`、natural `292 passed`、compileall 成功，八类 formal validators valid，bridge-v3 字节重放一致，既有保护哈希未漂移。该结果当时只解锁 L2 dev qualification；L2 已由下述 v9/v12 完成。

2026-07-28 typed extractor v2 L2 dev qualification 完成。最终 root 为 `artifacts/automatic-extraction-assessment/typed-extractor-v2-l2-dev-v9/`，passing run 为 `run-20260728T084011Z-deepseek-chat-official-typed-l2-dev-v12`。v7/v10 因独立复审发现 standalone threshold 与 raw/model provenance 审计缺口而降为历史审计；修复后的 v8/v11 以 provenance v2 绑定请求别名 `deepseek-chat`、raw response 返回模型 `deepseek-v4-flash`、raw/proposals 和 freeze sequence，但 raw 有 1 个 `structured_claim_error`，所以 raw fail、gate safe 分开保留。只在 dev prompt/public exact-copy contract 上修复后，v9/v12 的 decision、abstention F1、evidence、support、kind、structured claim、abstraction、closure、source coverage、summary 均为 `1.0`，critical false emission、gate intervention、deterministic critical false materialization 均为 `0`，双门通过。guard fingerprint 前后为 `e184b6caf2998acf1c8700bc84d24bbafd49ab9c8f15738d1af8cc484ee5ebcc`，automatic writes 全为 `0`，`LONGMEMEVAL-6d550036` 仍为 `structured_l2_identity_unresolved`。final proposals/provenance/score/report SHA-256 为 `655c696154d7e623ae417d3c5978a23ac7f7fb611e1b9f27ef21f7e25344e2c9`、`df905c9ed119eff40b39239243b53ef1ccfc6aced39b5b377854233fcc421d26`、`e27be9f426a78bf67fb03938ce97aeccc697e071bf866494d9a1d319fed4db57`、`c7cdd49adb166112c5a6ba6a64ef3c398f01c979bd4591c3135d911eafe8d600`，均为 `0444` 且重放一致。最终验证为 L2 focused `31 passed`、knowledge `197 passed`、natural `352 passed`、compileall 成功，formal validators/bridge replay/保护哈希均通过。下一步只预注册 fresh hidden typed-extraction evaluation，不授权 pipeline integration 或权威写入。

2026-07-28 fresh hidden 预注册前补齐 L1 provenance v2。官方 DeepSeek v7/v8 按 raw fail/gate safe 保留；prompt V6 只使用 12 个 dev case 的 unresolved-time、causal-modality、scope/condition 错误修复，v9 `run-20260728T091848Z-deepseek-chat-official-typed-l1-dev-v9` 双门通过。requested alias `deepseek-chat` 与 raw response model `deepseek-v4-flash` 分开绑定，dispatch/raw/proposals/provenance 顺序和 scorer 重验完整。v9 raw decision/abstention/evidence/kind/predicate/modality/time/derivation/operation 为 `1.0`，role/condition/lifecycle 为 `0.8888888888888888`；一个 lifecycle 错误被 gate 降为 abstain，gated accuracy 为 `0.9166666666666666`，该安全结果不得掩盖 raw error。v9 proposals/provenance/score/report SHA-256 为 `dd28482420680604a8ac85c10bf6506079420f882300393af1cb0fdb2bdfeac2`、`400217f3cb0343ceb2d61c03f905bc9f32147cf937e648f9497525adbecc52cc`、`e2bd3a3c613b927d4f065b8c2fbfd4d33fe43b8c05f6fa9c25b5173195443612`、`8837be4319a390024faea9eb04b12d990a44a3d10df51a371977bb9320ab72f1`。L2 通过只读 selection receipt（SHA-256 `27e5f46e1bf8614bc5573b388a573309117233f2ed1608c0fc6a72131fd70544`）继续绑定原 v6 L1 hash，不因目录新增 v9 而漂移。最终验证为 typed focused `58 passed`、knowledge `197 passed`、natural `364 passed`、compileall 成功，所有 validators valid，candidate v3 queue 未漂移、人工裁决未物化、密钥未落盘、automatic writes 全为 `0`。下一动作是在任何 hidden case 创作前冻结 fresh hidden 预注册协议。

2026-07-28 fresh-hidden v1 预注册已冻结。正式文件 `artifacts/automatic-extraction-assessment/typed-extractor-v2-fresh-hidden-prereg-v1/preregistration.json` 的 SHA-256 为 `9ec35f71c86c0e2c0257438d01445d9f0a490eee57a0d582ccb01024ffc0921c`，mode `0444`；冻结和验证时 future evaluation root 不存在，结果为 L1 24、L2 8、hidden absent。协议固定 prompt、threshold、namespace、bridge/source/dev exclusion、code hash、requested model 和一次性 semantic-run policy。L1 后续按公开 strata + hash 选择，L2 使用全部未用于 dev 的 8 条 cross-turn records；不得从 hidden 结果原地修复。仍不授权 pipeline 或任何 authoritative write。

远程执行 prompt：`docs/handoffs/2026-07-27-h100-ke-memory-next-handoff.md`。

2026-07-28 typed-extractor fresh-hidden v1 正式评测已关闭。L1 24 cases：raw quality fail（decision `0.7916666666666666`、abstention F1 `0.5714285714285715`、evidence `0.8947368421052632`、critical false emission `3`），gate safety pass（intervention `4`、critical materialization `0`）。L2 8 cases：raw quality fail（decision `0.625`、abstention F1 `0.0`、critical false emission `3`、abstraction `0.4`、structured claim `0.6`），gate safety pass（intervention `6`、critical materialization `0`）。两层请求 `deepseek-chat`、响应模型 `deepseek-v4-flash`；raw failure 不能被 gate pass 覆盖，不授权 pipeline integration 或权威写入。

总报告位于 `artifacts/automatic-extraction-assessment/typed-extractor-v2-fresh-hidden-v1/overall-report.md`。prereg/chronology、所有 hidden 输入、模型 run 与评分产物保持只读；bridge-v3 和两层评分三件套重放一致。fresh-focused `14 passed`、typed `72 passed`、knowledge `197 passed`，formal identity/benchmark/dev/fresh validators 通过。candidate v3 queue SHA 未漂移，四项人工裁决未物化，automatic writes 为 `0`，密钥未落盘，`LONGMEMEVAL-6d550036` 继续 unresolved。

下一执行序列调整为：先在新 dev/diagnostic 数据上修复 L1 false emission/abstention/evidence/qualifier/time/role/lifecycle 和 L2 abstention/abstraction/claim/kind；dev 双门通过后预注册并运行 fresh-hidden v2。并行问题编译/执行验证可以继续 candidate-only 开发，但不得消费 fresh-v1 失败结果作为权威输入。fresh-v2 通过后再进入抽取接入、证据闭包、跨 session 聚合与真实 benchmark 扩展，最后比较 Extended-AMR、KEOL projection 或其他 profile。

此前的 query compiler assessment blocker 已由并行工作补齐；当前新的 `test_query_execution_snapshot_adapter.py` 已出现但实现模块尚未落盘。该独立 collection blocker 不归因于 typed extractor，本阶段不修改 query 文件；排除它后的新鲜 natural-memory 回归为 `464 passed`，typed extractor 为 `105 passed`，knowledge pipeline 为 `197 passed`，compileall 成功。

2026-07-28 prompt-complete diagnostic public contract v2 baseline 已完成。旧 v1 catalog preflight fail，未发送模型请求且保持不可变。L1/L2 v2 roots 分别为 `typed-extractor-v2-l1-dev-repair-v2`、`typed-extractor-v2-l2-dev-repair-v2`，冻结 prompt SHA 分别为 `a4d03b0e...5586`、`55fddf35...60c7`；两层均使用无历史、public-only official `deepseek-chat` 请求，response model 均为 `deepseek-v4-flash`，proposal/provenance 在 authority/gold scoring 前冻结。

L1 strict 结果为 raw fail/gate pass：只有 role/local-entity 为 `0.9166666666666666`，其余 strict metric 为 `1.0`，错误是 `with Dana` 未拆出 participant，gate intervention `0`。L2 strict 结果为 raw fail/gate fail：abstraction/structured claim 均为 `0.875`，gate intervention `1`，错误是 persistent state 错选 coreference 与 archive coreference claim 复用 request sense。两层 critical false emission、false abstention、evidence error、critical materialization 和 automatic write 均为 `0`，guard fingerprint 未变；candidate v3 queue 未漂移，人工裁决未物化，LongMemEval identity 继续 unresolved。

下一步只在 diagnostic 数据上执行 prompt repair：L1 显式 participant 分解；L2 abstraction dominance 与 versioned `operator_sense_bindings`。双层 strict metric 全 `1.0` 且 intervention `0` 前不得预注册 fresh-hidden v2，也不授权 pipeline integration 或权威写入。

2026-07-28 diagnostic prompt repair 已完成。L2 v3 run `run-20260728T123925Z-deepseek-chat-official-typed-l2-dev-repair-v9` 与 L1 V10 run `run-20260728T130544Z-deepseek-chat-official-typed-l1-dev-repair-v10` 的 raw proposer quality、deterministic gate safety 和 strict dev-repair qualification 均通过；两层所有 strict exact metric 为 `1.0`，gate intervention、raw critical false emission、deterministic critical materialization 和 automatic writes 均为 `0`。L1 V7/V8/V9 的 raw failure 仍作为不可变诊断证据保留，未用 gate safety 覆盖。

L1 V10 prompt/proposals/provenance/score/qualification SHA-256 为 `b868bb2baaf1dfb3c27f99e2fe29888e2af0c57e56be3d2ecf70bfb6d2bcdfcf`、`7cca8c171d784fbee1ea4849c52403fa6446f3d78fe746cec4134a3effb95c39`、`8a2ef39bf2da9a059a24537505b05f5391a1806f5012642fa173864746dbf483`、`993200f235b075b4f7e5e72bf37d19cc8427d360ce75ab5375f91f3e7923ba43`、`377d91553db76fa1e3d48602e739aed255b107fca3856a35908234c769cb93a0`。下一步仅冻结 fresh-hidden v2 预注册，不直接创建 hidden、不接入 pipeline、不执行权威写入；guard fingerprint、candidate v3 queue 和 unresolved LongMemEval 边界保持不变。

最终验证：typed extractor `114 passed`、knowledge pipeline `197 passed`、完整 natural memory benchmark `485 passed`、`compileall` 成功；两层 replay/validator、只读权限、零写入、key-pattern `0` 均通过。额外 workspace hygiene `19 passed, 1 failed`，唯一失败为既有 Windows 反斜杠路径 fixture 在 Linux 上的错误优先级差异，未执行删除。fresh-hidden v2 root 仍不存在。

## QuerySlotPlan V2 TODO（2026-07-28）

- [x] 冻结表示无关的 Query draft、typed AST、plan hash、memory/ontology/identity/compiler revision binding 合同。
- [x] 实现 deterministic entity/predicate linking、gate safety、保守 V1 lowering 和 natural-query producer protocol。
- [x] 实现 AND/OR、多跳、canonical identity join/count、exact/latest、conflict/lifecycle/source/evidence executor。
- [x] 实现独立 formal assessment、Git chronology/memory-view 校验和 standalone `compile-batch`/`score-batch` CLI，不修改共享 `cli.py`。
- [x] 实现 verified Git execution snapshot adapter，校验 authoritative bundle、TurnBundle、current revision ownership、artifact commit/logical ID、L1/L2 evidence provenance 和 identity scope。
- [x] 完成 Query focused `55 passed`、相邻契约 `116 passed`、完整 natural-memory `485 passed` 及静态检查。
- [ ] 将 `CompiledQueryPlanV2.ontology_revision`、`identity_revision` 与 verified snapshot/registry 的实际 revision 做程序化一致性校验。
- [ ] 决定并实现 active L2 的 per-claim closure；在此之前继续拒绝多 structured-claim L2 execution mapping。
- [ ] 为 explicit absence 建立可执行 absence closure/fact contract；在完成前保持 `explicit_absence_unsupported` abstain。
- [ ] 为 `before/after/between/as_of`、literal term 和其他 answer projection 建立明确 executor 支持或 compile-time gate。
- [ ] 冻结自然 query dev slice、raw draft proposer/provenance、独立 gold/scorer；raw proposer quality 与 deterministic gate safety 分开报告。
- [ ] 预注册 fresh-hidden query compiler gate，验证 family coverage、false executable、false abstention、denotation、Evidence Set Exact Match 和 fixed-Git-ref replay。
- [ ] 串联 `natural query -> compiler -> verified snapshot -> executor -> evidence scorer -> answer/eval`，不得跳过 materialization、closure 或 Git checkpoint。
- [ ] 与 typed-extractor fresh-v2 并行但隔离；两条线均通过前不接 pipeline、不授权任何 authoritative write。

## Typed Extractor Fresh-Hidden V2 Preregistration（2026-07-28）

- [x] 在任何 authoring implementation/test、implementation receipt 和 future evaluation root 出现前，冻结独立 v2 prereg contract；未复用 fresh-v1 已耗尽的 L2 bridge remainder。
- [x] 固定 deterministic authored-hidden composition：L1 24 条（六个 family 各 4），L2 12 条（六个 family 各 2）；全部使用，禁止 semantic filtering、case replacement 和 post-generation resampling。
- [x] 绑定最终 L1 V10/L2 v9 prompts 与 passing-chain SHA、final dev/diagnostic/fresh-v1 exclusion、47 个 input hash、9 个 code hash、public-only/no-history、每层一次 semantic run、proposal freeze before scoring 和 raw/gated 分离报告。
- [x] 固定 L1/L2 14/12 个 quality metric 全部 exact `1.0`，raw critical false emission、gate intervention、deterministic critical materialization 全部为 `0`。
- [x] 首个 prereg-v2（SHA-256 `0ac045b0cbed26a2fb624bc1f70d8275ae94edb41a57e868bae78c6effadc243`）因 review 发现 coercive Pydantic types 和失实 mtime chronology 声明而 superseded；旧文件保持 `0444` 不改写。
- [x] 当前正式 prereg-v3 位于 `artifacts/automatic-extraction-assessment/typed-extractor-v2-fresh-hidden-prereg-v3/preregistration.json`；schema `typed-extractor-fresh-v2-preregistration-v2`，freeze time `2026-07-28T14:08:22Z`，SHA-256 `1455bb7d5bb35b61809c78180ebf766573c1561e39bbeddda4a80f939a893760`，size `11016` bytes，mode `0444`，validator 返回 L1 24/L2 12/future absent/hidden not created。
- [x] 本阶段 hidden/model request 和所有 automatic L1/L2/revision/source-revision/closure/identity/membership/snapshot/aggregate writes 为 `0`；guard fingerprint 与 candidate v3 queue 未漂移，人工裁决未物化，`LONGMEMEVAL-6d550036` 继续 unresolved。
- [x] hardening 后新鲜验证为 prereg focused `17 passed`、typed extractor `127 passed`、knowledge pipeline `197 passed`、完整 natural memory benchmark `498 passed`、`compileall` 成功、credential token pattern `0`；`.venv-h100` 未安装 Ruff，未修改环境。
- [x] 已实现 `typed_extractor_fresh_v2_authoring.py` 与对应测试；L1 24/L2 12 个 deterministic blueprint 全量使用，L2 primary claim literal binding 已覆盖 public predicate、subject、object/theme 和 support set。

## Typed Extractor Fresh-Hidden V2 Authoring Receipt（2026-07-29）

- [x] 保留提前冻结且绑定修复前 module 的 v1 receipt：mode `0444`，SHA-256 `4bb2575d985126dfbd91d01a3111d3f4acb2ebe57c3bfd62b1d60ee847d7314d`；不得删除、覆盖或作为 active authority 使用。
- [x] 冻结 append-only `authoring-implementation-receipt-v2.json`：freeze time `2026-07-29T00:30:16Z`，schema `typed-extractor-fresh-v2-authoring-receipt-v2`，size `7364` bytes，mode `0444`，SHA-256 `4ce77c20c2941a66c3e74c1bd561e9d5ceee360a3723b75eed694fc47267c94b`，supersession reason 为 `post_freeze_review_primary_literal_binding_repair`。
- [x] active resolver 已验证只选择 v2；current module/test、4 个 dependency、L1/L2 blueprint manifest、47 个 prior input hash、旧 receipt hash、formal-root absence 和九类 zero automatic writes 均闭合。
- [x] 第三轮独立只读复审无 Critical/Important；保留一个 Minor 回归覆盖建议。新鲜验证为 focused `49 passed`、typed extractor `177 passed`、knowledge pipeline `197 passed`、natural memory `580 passed`、`compileall` 成功。
- [x] prereg-v3、旧 receipt、candidate v3 queue 和 guard fingerprint 未漂移；credential pattern 为 `0`，formal root 仍不存在，无本项目活动 test/API/model session，人工裁决未物化，`LONGMEMEVAL-6d550036` 继续 unresolved。
- [x] 设计并执行 one-time formal hidden materialization；materializer 先调用 `validate_fresh_v2_active_authoring_receipt`，只冻结 source/public/authority/gold/manifest/chronology。materialization 前未调用 proposer/model，且未授权 pipeline integration 或任何 authoritative write。

## Typed Extractor Fresh-Hidden V2 Materialization（2026-07-29）

- [x] 独立 review 发现 active receipt 未固定批准 SHA 和 `os.replace` 可覆盖并发空 root；先新增 3 个失败回归测试，再实现固定 SHA 与 `renameat2(RENAME_NOREPLACE)`，re-review 无 Critical/Important。
- [x] 冻结前验证：authoring+materialization `64 passed`、typed `192 passed`、knowledge `197 passed`、natural `603 passed`，`compileall`、`tabnanny` 和 100 字符扫描通过；formal root/staging 缺失、credential pattern `0`、工作区活动 test/API/model process `0`。
- [x] 以 caller-supplied UTC label `2026-07-29T02:10:29Z` 执行唯一一次 no-clobber publish。正式 root 含 L1 24、L2 12、共 11 个 `0444` JSON；目录为 `0775`，无 `model-runs`、proposal、provenance、score 或 report。
- [x] chronology SHA-256 为 `278d9ba8f4d466984b11644de920b815de11cbdb56b49dff0dd797543b12b05e`；十个 payload deterministic replay `10/10` 字节一致。materializer module/test SHA 为 `57a8e6df5c6fc4739b50cf842f53063610f2c5bfc0ada21b84de0d87ab65a606` / `84f88ce70ca818a27b520b21d5a43a7f46a3bba8d2393bf0ee5417afc24191ac`。
- [x] 冻结后验证：focused `63 passed, 1 deselected`、typed `191 passed, 1 deselected`、knowledge `197 passed`、natural `602 passed, 1 deselected`。唯一 deselected 是 active-receipt 绑定的 pre-materialization-only root-absence 测试，不得修改该测试或 authoring module。
- [x] automatic writes、model request 和人工 identity adjudication materialization 均为 `0`；guard/candidate queue 未漂移，`LONGMEMEVAL-6d550036` 保持 `structured_l2_identity_unresolved`，不授权 pipeline integration。
- [x] 已运行隔离、无历史、public-only L1/L2 proposer，各层仅一次 official `deepseek-chat` semantic run，无 retry；raw response 均为 `deepseek-v4-flash`。proposals/provenance 在 scoring 读取 authority/gold 前冻结，proposal SHA-256 为 `5e0ac9cf0ed5dd50cf2bffa78add1e988d599856ba94820639e5f763527c4a05` / `59e1f052c2157782c1f8266d91a9ae319a2ba87cb7b065d58bf8a6adbd8a9da6`。
- [x] 独立 scoring 与 final prereg qualification 已完成，总体 `not_qualified`。L1 raw quality fail：decision `0.9583333333333334`、abstention F1 `0.8571428571428571`、evidence `1.0`、role/local entity `0.5555555555555556`、time `0.8888888888888888`、condition/scope `0.9444444444444444`、critical false emission `1`；L2 raw quality fail：decision/abstention/evidence/support/kind/source/summary `1.0`，structured claim `0.0`、abstraction `0.7777777777777778`、closure `0.5555555555555556`。两层 intervention 均为 `4`，所以尽管 critical materialization 均为 `0`，最终 deterministic gate safety 也均 fail。
- [x] compatibility/supersession receipt 已记录旧 scorer 的 manifest/threshold 和 guard 路径兼容处理，正式 manifest/proposals 未修改且未重跑模型；最终 L1 使用 canonical `score-v2.json`。L1/L2 score SHA 为 `f09d754e6848ade7fbbc0fc723899c63a7e24947c3c2cb6a13401ae1bbd0dbc8` / `6d656d9883803f1b8ca5a200babfa285547f98e99e048fc3908517dc31e8c092`。
- [x] 最终只读审计 `117/117` 通过，两个逐字节相同的 `.tmp` proposal 副本已清理；typed `191 passed, 1 deselected`、knowledge `197 passed`、natural `602 passed, 1 deselected`，`compileall`/`tabnanny` 通过，credential pattern `0`。automatic authoritative writes 为 `0`，人工裁决未物化，pipeline 未授权，LongMemEval identity 继续 unresolved。
- [ ] 只在新的 dev/diagnostic 数据上修复 L1 false emission、role/local entity、time、condition/scope，以及 L2 structured claim、abstraction、closure；raw strict metrics 全过且 gate intervention 为 `0` 后再预注册新的 fresh hidden。在此之前不得接 pipeline、closure、aggregate 或扩展 benchmark。

## Typed Extractor Taxonomy Dev V1（2026-07-29）

- [x] 在不读取或复用 fresh-v2 hidden case 内容的前提下，创建 taxonomy-only `diagnostic_authored` L1/L2 source，分别 20/12 case；formal source/manifest SHA-256 为 `d830246820c1df017baaace328111105f04858ce30f86d624b4a890be49d37b5` / `10292706e91d1eca7a0285fc83fd57cf0912ad51faff519fe54000864683029d` 和 `693cea99322c9589864749213ad1705ba6eabf3323bd3a19d6e5ec85d9a98418` / `bed50074ee41a3119a6e5803a0a24d403aa7557f75ceb8491c54ba132b31cdf8`。两层 prior identifier/evidence overlap 为 0，所有 source/public/authority/gold/manifest 均为 `0444`。
- [x] 冻结 unchanged-prompt、无历史、public-only official `deepseek-chat` baseline；每层仅一次请求，无 retry，raw response model 为 `deepseek-v4-flash`，并在 authority/gold scoring 前按 dispatch -> raw response -> proposals -> provenance 顺序冻结。L1 baseline raw strict 为 `not_qualified`，唯一失败 `role_or_local_entity_accuracy=0.9375`；deterministic gate safety 仍为 pass 且 intervention 为 0，不能替代 raw 结论。L2 baseline strict raw/gate 双门均 pass。
- [x] 仅按新 L1 diagnostic taxonomy 生成 repair-v1 prompt：不含任何完整 diagnostic message 或 private case ID。repair-v1 的 14 项 strict raw metrics 均为 `1.0`，raw critical false emission、gate intervention、deterministic critical materialization 均为 0；L2 因 unchanged baseline 已通过而不增加无意义的 revision。repair prompt/proposals/score/qualification SHA-256 为 `a5250a453863f3cfd388613d6633d8a529485a11c5226d2965c8235a9c1dc342` / `db468cc0251abe48bfb1c98dead13471b16e32556df3a61da528e3e29de95750` / `dfbb8a415e6654b89f4ac09e0ae72bc45d1687cec13e69cbd2211f6ef295bb92` / `92e627fb72ba18ef1458aa9ef810bf8bf9b5efad3a12cc0f7559231e07a3c3de`。
- [x] 完成 67 项只读审计：formal/model-run files 均 `0444`、manifest/chronology/provenance/hash/replay 闭合、guard fingerprint 仍为 `e184b6caf2998acf1c8700bc84d24bbafd49ab9c8f15738d1af8cc484ee5ebcc`、candidate v3 queue SHA 仍为 `518ead9de8627a9a4384df8cdd9b8728a21fcf797f6b0f3d208dfcb28ac41c0f`、credential pattern 与 automatic writes 均为 0。新鲜回归：taxonomy focused `19 passed`、typed `210 passed, 1 deselected`、knowledge `197 passed`、natural `637 passed, 1 deselected`，`compileall`/`tabnanny` pass；唯一 deselection 是既有 pre-materialization root-absence test。

### Typed extractor fresh-hidden v3 preregistration（2026-07-29）

- [x] 在 v3 authoring/materialization code/test、receipt、evaluation root 全部不存在时先冻结 prereg；formal root 只含一个 mode `0444` 的 `preregistration.json`，SHA-256 `183cf6fc2991361e5da57b17e06a5970f651000986d50b87e8af2e6796440204`，size `10998` bytes，freeze label `2026-07-29T04:51:35Z`。
- [x] validator 返回 valid、L1 `24`、L2 `18`、future absent、hidden not created、model request `0`。L1 八个 taxonomy family 各 3 条，L2 九个 family 各 2 条；all-blueprint/no-filter/no-replacement/no-resampling 合同已冻结。
- [x] 绑定 taxonomy L1 repair-v1、L2 unchanged baseline、fresh-v2 exclusions：39 个 read-only input hash、11 个 code/test hash、14/12 项 exact-1.0 quality gate 和三项 zero safety count。no-history/public-only、proposal-before-scoring、raw/gated separate-reporting 继续是后续硬边界。
- [x] 最终回归为 v3 focused `16 passed`、adjacent prereg `34 passed`、typed `226 passed, 1 deselected`、knowledge `197 passed`、natural `653 passed, 1 deselected`，`compileall`/`tabnanny` pass。candidate queue 与 guard hash 未漂移，credential pattern 和 automatic writes 为 `0`。
- [x] deterministic v3 authoring 与 normalized-snapshot relocation receipt 已完成；首个 `2026-07-29T13:42:32Z` 匿名 inode 尝试因 JuiceFS ghost 失败且不算正式 freeze，修复后独立 reviewer 无遗留问题。
- [x] 唯一正式 receipt 于 `2026-07-29T14:35:02Z` 冻结：schema `typed-extractor-fresh-v3-authoring-receipt-v2`，SHA-256 `c810f421d5a3b0726b862ec2f12c89e0d638e0747892637e2a32977587b7ef8c`，size/mode/nlink 为 `9370` / `0444` / `1`，L1/L2 `24/18`、39 个 input、model request 与九类 automatic write 均为 `0`。Git recovery chain 固定 commit `00fa803ee44bcef5a299babb9a8e2b7ba9f994e4` 和三个 imported blobs。
- [x] pre-freeze gate 为 relocation `23 passed`、adjacent authoring `77 passed`、phase-aware typed `310 passed, 1 deselected`、v3 prereg phase `5 passed, 11 deselected`、runtime `773 passed, 1 skipped`；static、credential、candidate queue/live guard 与 zero-write audit 通过。knowledge 缺 `nltk` 和 natural broad 的旧 phase/`duckdb`/旧绝对路径 gaps 单独保留，不算通过。
- [x] post-freeze validator/exact audit valid；phase-aware typed `389 passed, 15 deselected`、runtime `773 passed, 1 skipped`。正确 prep cwd 的 tracked natural 为 `832 passed, 24 failed, 15 deselected`，24 项均是既有缺 `duckdb`/冻结旧绝对路径 gap；knowledge 因缺 `nltk` 为 6 个 collection error。static、Git recovery chain、credential `0`、formal-root exactness、candidate queue/live guard、future absence 和 zero-write audit 通过。
- [x] one-time fresh-v3 hidden materialization 已于 `2026-07-30T04:44:31Z` 唯一正式执行；official root 为 `typed-extractor-v3-fresh-hidden-v1`，public validator `valid`，L1/L2 `24/18`，11 个 JSON 为 `0444`、root/layer 为 `0775`。chronology SHA-256 为 `fe3cfbd739de477d99089c4ed6f85322236e00deb9b405596f038366bee16cca`，materializer/test SHA-256 为 `1e134fcc005da9e10f9f5be95cf46f3451b39f16556b1a088c9f034d02f6bc7e` / `76f5e2377b153b68eef1d5a84be7a865af8a24cbf30b4e5adb7b49915646ab80`，payload replay `10/10` byte-identical，无 model/result/staging 路径。
- [x] 独立 rereview 为 Critical/Important/Minor `0/0/0`。pre-freeze gate 为 focused `45 passed`、phase-aware prereg/authoring/snapshot `54 passed, 62 deselected`、typed `255 passed, 1 deselected`、runtime `773 passed, 1 skipped`、natural `775 passed, 24 allowlisted failures, 1 deselected`；knowledge 精确保留 6 个缺 `nltk` collection error。post-freeze 精确 phase rerun 为 `61 passed, 100 deselected`；validator、11-file audit、static、credential `0`、candidate queue/live guard、parallel SHA 和九类 zero-write checks 通过。
- [ ] 先单独设计 no-history/public-only proposer-freeze/qualification，并取得用户明确批准后再执行。当前不授权 proposer、scorer、authority/gold scoring、pipeline integration、authoritative write、closure、aggregation、benchmark 扩展或外部系统复跑；manual identity adjudication 不物化，embedding 非权威，`LONGMEMEVAL-6d550036` 保持 `structured_l2_identity_unresolved`。

### Typed extractor fresh-hidden v3 qualification（2026-07-30）

- [x] proposer-freeze/qualification 设计、实现与 preflight 已完成；两层 dispatch 在任何请求前冻结，正式 label 为 `20260730T061218Z`。
- [x] L1/L2 各执行唯一一次 `deepseek-chat` HTTP 请求，请求数 `1/1`；两层均以 `HTTPError: HTTP Error 403: Forbidden` 结束，无 retry、fallback 或第二次请求。
- [x] 冻结两份 failure receipt 和 overall incomplete conclusion；最终为 `incomplete_not_qualified`。没有 raw response、proposal、provenance、score 或 layer qualification，scorer 未读取 authority/gold，九类 automatic write 为 `0`。
- [x] 不含评测数据的 API probe 已定位根因：key 有效，`deepseek-v4-pro` 可访问，但 `deepseek-chat` 被 `key_model_access_denied` 拒绝。旧正式记录保持不可变。
- [ ] 自动抽取质量仍未测量。若按用户新授权以 `deepseek-v4-pro` 重测，必须创建独立的新 run/qualification 事实链，不得把旧 403 run 改写为成功，也不得借此进入 pipeline integration 或 authoritative write。
