# QueryDraftV1 deterministic dev qualification policy

Input is limited to opaque public cases. Authority, registries, gold, history, and typed-extractor hidden data are unavailable.

Emit one strict QueryDraftV1 JSON object per case. Use group-0 and group-1 in document order, atom-0 onward in document order, and stable variable bindings. Do not repair, answer, write memory, or treat embedding output as authority.

This dev reference is emitted by a deterministic harness. It is not a semantic model run and is qualification evidence only.
