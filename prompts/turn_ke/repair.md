The prior turn-extraction response failed the required structured schema or local contract.
Using the unchanged source context, validated draft, and candidate bundles, return one
corrected JSON object for the active response schema. Preserve exact Unicode offsets and
proposal-local references. Do not add ontology IDs beyond offered matching-role document
IDs, and use only the supplied unresolved marker when no typed candidate is selected.
